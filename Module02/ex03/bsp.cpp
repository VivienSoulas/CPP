#include "Point.hpp"

bool bsp( Point const a, Point const b, Point const c, Point const point )
{
	t_node tree;

	
	// create a tree
	// split view om x axis
	// check if x is hit
	// split in y
	// check if y is hit	
}